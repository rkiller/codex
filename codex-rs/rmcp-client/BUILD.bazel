load("//:defs.bzl", "codex_rust_crate")

codex_rust_crate(
    name = "rmcp-client",
    compile_data = glob(["src/oauth_callback_assets/*"]),
    crate_name = "codex_rmcp_client",
    extra_binaries = [
        "//codex-rs/cli:codex",
    ],
)
